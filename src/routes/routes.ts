export const enum Routes {
  base = "/",

  //auth
  auth = "/auth",
  login = "/auth/login",
  signUp = "/auth/signup",
  passwordReset = "/auth/password-reset",

  // dashboard
  dashboard = "/dashboard",
}
